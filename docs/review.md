# 验收记录

## 已验证

- 14 个 Node 单元测试通过
- 服务端与小程序 JavaScript 语法检查通过
- JSON 配置解析通过
- WXML 结构检查通过
- Docker Compose / GitHub Actions YAML 解析通过
- SQLite upsert、提醒状态与历史清理 SQL smoke test 通过
- 奉贤 5 号楼映射为 `areaid=2, buildid=27`
- 徐汇南区 4A 映射为 `areaid=3, buildid=68`
- 电量解析覆盖直接数值、带“剩余电量”标签、无效页面
- 充值跳变不会计入耗电
- 查询缓存有容量上限
- 学校请求有全局并发上限
- 同寝室多人关注合并采样
- 最后一个关注者取消后清理该寝室历史
- Session 30 天过期并定时清理
- 历史数据默认 14 天清理
- 微信 access_token 缓存、并发刷新和失效重试
- `43101` 清空不可用订阅次数，`47003` 保留次数并记录配置错误
- 定时采样防重入

## 外部依赖

- 学校查询地址与楼栋映射已和公开的 ECUST 电量监控项目核对
- 微信订阅消息调用结构已按当前订阅消息接口核对
- 真机登录、模板字段和消息送达需使用实际 AppID、AppSecret、模板 ID 验收
- 当前执行环境无法解析 npm registry DNS，未完成本机依赖安装和 Docker 构建；仓库 CI 会执行完整依赖安装、测试与应用导入检查
- 当前执行环境无法直接访问华理电量页面，未进行真实寝室在线请求
